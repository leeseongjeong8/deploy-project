CREATE TABLE DEPLOY.DEPLOY_MANAGEMENT (
	DEPLOY_NO NUMBER NOT NULL,
	DEPLOY_TITLE VARCHAR(100) NOT NULL,
	WRITER VARCHAR(20) NOT NULL,
	DEPLOY_DATE DATE NOT NULL,
	DEPLOY_CONTENT BLOB NOT NULL,
	EXPECTED_DATE DATE NOT NULL,
	DEPLOY_STATE VARCHAR(20) NOT NULL,
	STATE_REASON VARCHAR(500)
)
TABLESPACE USR
PCTFREE 10
INITRANS 2
STORAGE (
	MAXEXTENTS UNLIMITED
)
LOGGING
NOPARALLEL;
COMMENT ON TABLE DEPLOY.DEPLOY_MANAGEMENT IS '배포관리';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.DEPLOY_CONTENT IS '배포내용';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.DEPLOY_DATE IS '작성일';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.DEPLOY_NO IS '배포번호';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.DEPLOY_STATE IS '배포상태';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.DEPLOY_TITLE IS '배포제목';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.EXPECTED_DATE IS '배포예상날짜';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.STATE_REASON IS '배포상태이유';
COMMENT ON COLUMN DEPLOY.DEPLOY_MANAGEMENT.WRITER IS '작성자';

CREATE UNIQUE INDEX DEPLOY.DEPLOY_CON4482100084 ON DEPLOY.DEPLOY_MANAGEMENT (
	DEPLOY_NO ASC
)
LOGGING
TABLESPACE USR
PCTFREE 10
INITRANS 2;

ALTER TABLE DEPLOY.DEPLOY_MANAGEMENT ADD 
PRIMARY KEY (
	DEPLOY_NO
);