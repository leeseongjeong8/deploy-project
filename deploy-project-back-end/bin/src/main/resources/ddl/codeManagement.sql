CREATE TABLE DEPLOY.CODE_MANAGEMENT (
	CODE_ID VARCHAR(20) NOT NULL,
	CODE_NAME VARCHAR(20) NOT NULL,
	PARENT_CODE_ID VARCHAR(20),
	CODE_USE_YN VARCHAR(20) NOT NULL,
	REGDATE DATE NOT NULL,
	REGISTERER VARCHAR(20) NOT NULL,
	UPDATE_DATE DATE,
	MODIFIER VARCHAR(20),
	DSPL_ORDER NUMBER NOT NULL
)
TABLESPACE USR
PCTFREE 10
INITRANS 2
STORAGE (
	MAXEXTENTS UNLIMITED
)
LOGGING
NOPARALLEL;
COMMENT ON TABLE DEPLOY.CODE_MANAGEMENT IS '코드관리';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.CODE_ID IS '코드아이디';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.CODE_NAME IS '코드이름';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.CODE_USE_YN IS '사용여부';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.DSPL_ORDER IS '순서';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.MODIFIER IS '수정자';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.PARENT_CODE_ID IS '부모코드아이디';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.REGDATE IS '등록일';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.REGISTERER IS '등록자';
COMMENT ON COLUMN DEPLOY.CODE_MANAGEMENT.UPDATE_DATE IS '수정일';

CREATE UNIQUE INDEX DEPLOY.DEPLOY_CON4479700233 ON DEPLOY.CODE_MANAGEMENT (
	CODE_ID ASC
)
LOGGING
TABLESPACE USR
PCTFREE 10
INITRANS 2;

ALTER TABLE DEPLOY.CODE_MANAGEMENT ADD 
PRIMARY KEY (
	CODE_ID
);

ALTER TABLE DEPLOY.CODE_MANAGEMENT ADD CONSTRAINT FK_CODE_MANAGEMENT_REGISTERER_
FOREIGN KEY (
	REGISTERER
) REFERENCES DEPLOY.MEMBER (
	ID
);

ALTER TABLE DEPLOY.CODE_MANAGEMENT ADD CONSTRAINT FK_CODE_MANAGEMENT_MODIFIER_ME
FOREIGN KEY (
	MODIFIER
) REFERENCES DEPLOY.MEMBER (
	ID
);
