CREATE TABLE DEPLOY.LOGIN_HISTORY (
	HISTORY_NO NUMBER NOT NULL,
	ID VARCHAR(20) NOT NULL,
	STATUS VARCHAR(6) NOT NULL,
	REGDATE TIMESTAMP(6) NOT NULL
)
TABLESPACE USR
PCTFREE 10
INITRANS 2
STORAGE (
	MAXEXTENTS UNLIMITED
)
LOGGING
NOPARALLEL;

CREATE UNIQUE INDEX DEPLOY.HISTORY_NO_PK ON DEPLOY.LOGIN_HISTORY (
	HISTORY_NO ASC
)
LOGGING
TABLESPACE USR
PCTFREE 10
INITRANS 2;

ALTER TABLE DEPLOY.LOGIN_HISTORY ADD CONSTRAINT HISTORY_NO_PK
PRIMARY KEY (
	HISTORY_NO
);

ALTER TABLE DEPLOY.LOGIN_HISTORY ADD 
FOREIGN KEY (
	ID
) REFERENCES DEPLOY.MEMBER (
	ID
);
